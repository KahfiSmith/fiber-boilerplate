{
  "name": "fiber-boilerplate-harness",
  "version": "0.1.0",
  "private": true,
  "description": "Verification harness scripts for the fiber-boilerplate backend (no runtime deps).",
  "scripts": {
    "verify:fast": "go build ./... && go vet ./... && go test ./... && node ./scripts/harness/check-docs.mjs",
    "verify": "pnpm verify:fast",
    "verify:risk": "node ./scripts/harness/check-risk.mjs",
    "verify:cross-repo": "node ./scripts/harness/check-cross-repo.mjs",
    "verify:all": "pnpm verify && pnpm verify:risk && pnpm verify:cross-repo"
  }
}
